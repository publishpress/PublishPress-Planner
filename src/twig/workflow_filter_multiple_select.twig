<label for="{{ id }}">{{ labels.label }}</label>

<select name="{{ name }}[]" id="{{ id }}" multiple>
	{% for option in options %}
		<option
			value="{{ option.value }}"
			{{ selected( option.selected, true ) }}>

			{{ option.label }}
		</option>
	{% endfor %}
</select>
