<div>
	{% if event_filters %}
		<ul class="{{ id }}_filters">
			{% for event_filter in event_filters %}
				<li>
					{{ event_filter.render() }}
				</li>
			{% endfor %}
		</ul>
	{% endif %}
</div>
